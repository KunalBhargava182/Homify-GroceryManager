<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="@color/white"
    app:strokeColor="#ECEFF1"
    app:strokeWidth="1dp"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tvItemName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Fresh Milk"
                android:textColor="#37474F"
                android:textSize="18sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                android:maxLines="1"
                android:ellipsize="end" />

            <ImageView
                android:id="@+id/ivReminderStatus"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginEnd="8dp"
                android:contentDescription="Reminder Active"
                android:src="@drawable/ic_notification_on"
                app:tint="#90A4AE" />

            <ImageView
                android:id="@+id/ivOptions"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:contentDescription="Options"
                android:src="@drawable/ic_more_vert"
                app:tint="#546E7A" />
        </LinearLayout>

        <TextView
            android:id="@+id/tvQuantity"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="2 Liters"
            android:textColor="#78909C"
            android:textSize="14sp"
            android:textStyle="bold" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="12dp"
            android:background="#F5F5F5" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <com.google.android.material.chip.Chip
                android:id="@+id/chipExpiry"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="Exp: 20 Oct"
                android:textColor="#0277BD"
                android:textSize="12sp"
                android:textStyle="bold"
                app:chipBackgroundColor="#E1F5FE"
                app:chipStrokeWidth="0dp"
                app:chipIcon="@drawable/ic_calendar"
                app:chipIconSize="16dp"
                app:chipIconTint="#0277BD"
                app:ensureMinTouchTargetSize="false"
                android:clickable="false" />

            <Space
                android:layout_width="8dp"
                android:layout_height="wrap_content" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chipExpectedFinish"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="Lasts 2d"
                android:textColor="#455A64"
                android:textSize="12sp"
                android:textStyle="bold"
                app:chipBackgroundColor="#ECEFF1"
                app:chipStrokeWidth="0dp"
                app:chipIcon="@drawable/ic_timer"
                app:chipIconSize="16dp"
                app:chipIconTint="#546E7A"
                app:ensureMinTouchTargetSize="false"
                android:clickable="false" />

        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>